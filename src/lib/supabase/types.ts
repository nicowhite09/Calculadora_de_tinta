// Placeholder de tipos generados desde el esquema de Supabase.
//
// A partir de la Fase 1, reemplaza este archivo generando los tipos reales
// con la CLI de Supabase:
//
//   npx supabase login
//   npx supabase gen types typescript --project-id <tu-project-ref> > src/lib/supabase/types.ts
//
// Mientras tanto, este tipo mínimo evita errores de TypeScript en el cliente.
export type Database = Record<string, unknown>;
